apiVersion: v1
kind: Service
metadata:
  name: {{ include "mask-db.fullname" . }}
  labels:
    {{- include "mask-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mask-db.selectorLabels" . | nindent 4 }}
