apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mask-db.fullname" . }}-test-connection"
  labels:
    {{- include "mask-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mask-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
