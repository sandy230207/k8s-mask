apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mask-server.fullname" . }}-test-connection"
  labels:
    {{- include "mask-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mask-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
