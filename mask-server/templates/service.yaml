apiVersion: v1
kind: Service
metadata:
  name: {{ include "mask-server.fullname" . }}
  labels:
    {{- include "mask-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mask-server.selectorLabels" . | nindent 4 }}
