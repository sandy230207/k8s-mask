apiVersion: v1
kind: Service
metadata:
  name: mask-nodeport
spec:
  type: NodePort
  ports:
  - port: 3306
    nodePort: 30006 # The range of valid ports is 30000-32767
    protocol: TCP
    targetPort: 3306
  selector:
    app: mysql