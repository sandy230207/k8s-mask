apiVersion: v1
kind: Service
metadata:
  name: mask-server
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  # externalIPs:
  # - 35.194.136.172
  selector:
    name: mask-server